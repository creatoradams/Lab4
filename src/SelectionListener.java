// interface for any panel that reacts to selection changes
public interface SelectionListener
{
   // this is called when the usre picks a new country or years filter
    void selectionChanged(Selection event);
}
